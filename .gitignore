# ── Sensitive: the logged-in browser session (MOOPS / Admin / LaundroPortal / SF + Okta
# cookies). NEVER commit this. ──────────────────────────────────────────────────────────
chrome_profile/

# Slack incoming-webhook URL (secret, post-only) -- local fallback file. NEVER commit.
slack_webhook.txt

# Downloaded per-order customer config files (.cfg) -- customer data, regenerated per run
vac_configs/

# Generated / run artifacts
run.log
# append-only per-run audit trail; grows locally, persisted via OneDrive
action_log.jsonl
# reader-kit assessment log written by intake (per-run output)
reader_kit_assessments.jsonl
_efs_snippet.js
intake_board.html
intake_plan.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Office / LibreOffice lock files
.~lock.*#

# ── SF Deduper — a SEPARATE project that shares this base folder; keep its files OUT of this repo ──
sf_audit/
sf_data.js
sor_dedupe.sql
SF_*.xlsx
SF_*.csv
Shipped_Orders_*.xlsx

# Generated dedupe-board DATA -- regenerated per run, contains live customer PII
# (emails / phones / addresses). NEVER commit. The board SHELL (dedupe_board.html) and
# DEDUPE_RUNBOOK.md ARE tracked and shipped; the coworker generates the data locally.
dedupe_data.js
dedupe_keys.json
dedupe_results.json
dedupe_scope.json
